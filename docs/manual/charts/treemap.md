---
title: "Treemap"
anchors:
  - charts.treemap
---

# Treemap

`src.visualization.advanced_charts.TreemapChart`. **Required:** one or two categorical path
columns (outermost first, for a two-level hierarchy) and a numeric value column, summed per
leaf. Value must be non-negative -- a treemap segment cannot have negative area.
